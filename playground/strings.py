# We can sort things alphabetically by comparing strings.
# This is because, A < B in the ASCII table.

a = "Apple"
b = "Bear"

result = a < b

print(
  "Is the string %s < %s? %s" %
  (a, b, result)
)
